# Purpose:        Runtime configuration for the synthetic customer module.
# Responsibility: Provide connection details and feature flags. Used to exercise
#                 the config extractor and its secret-redaction (build plan §47).
# Notes:          db.password / db.connectionString MUST be redacted in the index.

app.name=synthetic-customer-service
app.batch.chunkSize=500

db.url=jdbc:postgresql://localhost:5432/customerdb
db.username=customer_app
db.password=sup3rs3cret-do-not-index
db.connectionString=jdbc:postgresql://localhost:5432/customerdb?user=customer_app&password=sup3rs3cret

metadata.tableRegistry=META_TABLE_REGISTRY
metadata.columnRegistry=META_COLUMN_REGISTRY

feature.eligibility.enabled=true
