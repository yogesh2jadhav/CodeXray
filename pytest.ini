[pytest]
# CodeXray test config. Tests import the `backend` package from the repo root,
# so the repo root must be on sys.path (rootdir + this file guarantees it).
testpaths = tests
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
