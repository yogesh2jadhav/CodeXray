# CodeXray backend dependencies — all run locally, no cloud services.

# --- Web API ---
fastapi==0.115.2
uvicorn[standard]==0.30.6
pydantic==2.9.2

# --- Config ---
pyyaml==6.0.2
python-dotenv==1.0.1

# --- Java AST parsing (primary). Regex fallback is used if these are missing. ---
# tree-sitter-java ships wheels for Python 3.9-3.13 (the old tree-sitter-languages
# bundle does not build on 3.12+). backend/app/analyzers/tree_sitter_setup.py also
# accepts tree-sitter-language-pack or the legacy bundle if either is present.
tree-sitter>=0.23,<0.26
tree-sitter-java>=0.23,<0.25

# --- SQL parsing (real parser, not the LLM) ---
sqlglot==25.24.0

# --- Sprint 4: dependency graph + semantic retrieval ---
networkx==3.3
numpy==2.1.3
# Embeddings run through the local Ollama server (nomic-embed-text); a pure-python
# hashing embedder is the offline fallback, so no extra ML deps are required.

# --- Testing ---
pytest==8.3.3
httpx==0.27.2

# Optional extras (not required to run CodeXray):
#   neo4j          — only for `scripts/export_graph.py --load` (direct bolt import).
#                    The .cypher / .graphml / .dot exports work without it.
#   qdrant-client  — swap-in vector store
#   sentence-transformers — alternative embedder
#   gitpython      — Sprint 8 (git integration)
